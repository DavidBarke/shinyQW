#' @export
mutate_ui <- function(input, output, session, user_data_storage, permanent_data_storage, values,
                      parent, selected_data, ...) {
  self <- node$new("mutate", parent, session)

  ns <- session$ns
}
