#' @export
arrange_ui <- function(input, output, session, user_data_storage, permanent_data_storage, values,
                       parent, selected_data, ...) {
  self <- node$new("arrange", parent, session)

  ns <- session$ns
}
